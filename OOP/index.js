// ----------- Все методы ООП в одном примере -------------------

// Создание класса "Животное"
class Animal {
    #sex; // Приватное свойство для демонстрации инкапсуляции
    constructor(name, age, sex) {
        this.name = name;
        this.age = age;
        this.#sex = sex;
    }

    // Метод для издания звука
    makeSound() {
        console.log(`${this.name} издает звук.`);
    }

    // Метод для получения информации о животном
    getInfo() {
        return `${this.name} - возраст: ${this.age} лет.`;
    }

    // Метод для получения пола животного (демонстрация инкапсуляции)
    getSex() {
        return `${this.name} - пол: ${this.#sex}.`;
    }

    // Метод для установки пола животного (демонстрация инкапсуляции)
    setSex(sex) {
        this.#sex = sex
    }
}

// Создание подкласса "Собака", наследующего от "Животное"
class Dog extends Animal {
    constructor(name, age, breed) {
        super(name, age); // Вызов конструктора родительского класса
        this.breed = breed;
    }

    // Переопределение метода для издания звука
    makeSound() {
        console.log(`${this.name} гавкает.`);
    }

    // Новый метод для получения информации о породе
    getBreedInfo() {
        return `${this.name} - порода: ${this.breed}.`;
    }
}

// Создание подкласса "Кошка", наследующего от "Животное"
class Cat extends Animal {
    constructor(name, age, color) {
        super(name, age); // Вызов конструктора родительского класса
        this.color = color;
    }

    // Переопределение метода для издания звука
    makeSound() {
        console.log(`${this.name} мяукает.`);
    }

    // Новый метод для получения информации о цвете
    getColorInfo() {
        return `${this.name} - цвет: ${this.color}.`;
    }
}

// Создание экземпляров классов
const dog = new Dog('Рекс', 5, 'Немецкая овчарка');
const cat = new Cat('Мурка', 3, 'Серый');

// Использование методов
dog.makeSound(); // Рекс гавкает.
console.log(dog.getInfo()); // Рекс - возраст: 5 лет.
console.log(dog.getBreedInfo()); // Рекс - порода: Немецкая овчарка.

// применение методов инкапсуляции
dog.setSex('Мужской');
console.log(dog.getSex()); // Мужской

cat.makeSound(); // Мурка мяукает.
console.log(cat.getInfo()); // Мурка - возраст: 3 лет.
console.log(cat.getColorInfo()); // Мурка - цвет: Серый.

// применение методов инкапсуляции
cat.setSex('Женский');
console.log(cat.getSex()); // Женский

// Демонстрация полиморфизма
const animals = [dog, cat];
animals.forEach(animal => animal.makeSound());

/*
| 🧩 Принцип                     | 💬 Что означает                                                | ⚙️ Как реализуется в JS                                | 💡 Пример кода                                                   | ✅ Есть нативно?  |
| ------------------------------ | -------------------------------------------------------------- | ------------------------------------------------------ | ---------------------------------------------------------------- | ---------------- |
| **Инкапсуляция**               | Скрываем внутренние данные, предоставляем только нужное наружу | Приватные поля `#`, замыкания, геттеры/сеттеры         | `js class User { #pwd = '123'; getPwd() { return this.#pwd; } }` | ✅ (через `#`)    |
| **Наследование**               | Один класс наследует свойства и методы другого                 | `extends` + `super()`                                  | `js class Dog extends Animal { speak() { super.speak(); } }`     | ✅                |
| **Полиморфизм**                | Один интерфейс — разные реализации                             | Переопределение методов в наследниках                  | `js class Cat extends Animal { speak(){console.log('Meow')}}`    | ✅                |
| **Абстракция**                 | Выделяем только суть, без реализации                           | "Базовые классы" с методами-заглушками                 | `js class Shape { draw(){ throw 'Must implement'; } }`           | ⚙️ (эмулируется) |
| **Композиция**                 | Объект состоит из других объектов (has-a)                      | Вложенные объекты / инъекция зависимостей              | `js class Car { constructor(engine){ this.engine = engine; } }`  | ✅ (логически)    |
| **Инстанцирование**            | Создание экземпляров класса                                    | `new ClassName()`                                      | `js const u = new User();`                                       | ✅                |
| **Модификаторы доступа**       | Контроль видимости (public/private)                            | `#private`, `_protected` (условно)                     | `js #secret; _semiPrivate;`                                      | ✅ (частично)     |
| **Переопределение (override)** | Замена метода родителя в наследнике                            | Переопределяем одноимённый метод                       | `js speak(){ console.log('bark'); }`                             | ✅                |
| **Перегрузка (overload)**      | Несколько методов с одним именем, разными аргументами          | Не поддерживается нативно (эмулируется через проверки) | `js add(a,b){ if(b) return a+b; return a; }`                     | ❌                |
| **Интерфейсы**                 | Определение структуры без реализации                           | Эмулируется через абстрактные классы / TypeScript      | `ts interface Animal { speak(): void }`                          | ❌ (есть в TS)    |

 */